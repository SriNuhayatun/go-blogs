@model List<Blog>

<h1>Details</h1>

@foreach (var item in ViewBag.detail)
{
    <h3>@item.Id</h3>
    <h3>@item.Title</h3>
    <h3>@item.CreateDate</h3>
    <h3>@item.Status</h3>
    <img src="@item.Image"  />
}
